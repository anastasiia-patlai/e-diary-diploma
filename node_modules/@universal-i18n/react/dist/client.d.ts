import * as react_jsx_runtime from 'react/jsx-runtime';
import { ReactNode } from 'react';

/** Pre-built array of all supported language codes. Pass this to `availableLocales` to enable all languages. */
declare const ALL_LOCALES: string[];
interface AutoTranslateProviderProps {
    /** Lingo.dev API key (ignored if fetchAction is provided) */
    apiKey?: string;
    /** The locale the site is originally written in (default: 'en') */
    sourceLocale?: string;
    /** The preferred locale to translate into on load */
    targetLocale?: string;
    /** List of allowed locales, or "all" to allow all supported locales */
    availableLocales?: string[] | "all";
    /** Delay in ms to batch text node changes (default: 150) */
    batchDelayMs?: number;
    /** CSS selectors to ignore during translation */
    skipSelectors?: string;
    /** Whether to show the floating language switcher (default: true) */
    showSwitcher?: boolean;
    /** Callback when locale changes */
    onLocaleChange?: (locale: string) => void;
    /** The path to your Next.js API route that proxies the Lingo.dev API. (e.g. '/api/universal-i18n') */
    apiRoute?: string;
    /** Custom fetcher (e.g. Next.js Server Action) to proxy translations securely */
    fetchAction?: (texts: Record<string, string>, source: string, target: string) => Promise<Record<string, string>>;
    children: ReactNode;
}
interface AutoTranslateContextValue {
    locale: string;
    sourceLocale: string;
    setLocale: (locale: string) => void;
    availableLocales: string[];
    isTranslating: boolean;
}
declare function useAutoTranslate(): AutoTranslateContextValue;
declare function AutoTranslateProvider({ apiKey, sourceLocale, targetLocale: initialTargetLocale, availableLocales, batchDelayMs, skipSelectors, showSwitcher, onLocaleChange, apiRoute, fetchAction, children, }: AutoTranslateProviderProps): react_jsx_runtime.JSX.Element;

export { ALL_LOCALES, AutoTranslateProvider, type AutoTranslateProviderProps, useAutoTranslate };
